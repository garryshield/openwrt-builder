name: Build OpenWrt Firmware

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  Build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # - platform: immortalwrt
          #   version: 24.10.2
          #   target: x86/64/generic
          - platform: openwrt
            version: 24.10.2
            target: x86/64/generic

    steps:
      - name: Generate Build Variables
        run: |
          echo "PLATFORM=${{ matrix.platform }}" >> $GITHUB_ENV
          echo "VERSION=${{ matrix.version }}" >> $GITHUB_ENV

          TARGET=$(echo "${{ matrix.target }}" | cut -d'/' -f1)
          SUBTARGET=$(echo "${{ matrix.target }}" | cut -d'/' -f2)
          PROFILE=$(echo "${{ matrix.target }}" | cut -d'/' -f3)
          echo "TARGET=$TARGET" >> $GITHUB_ENV
          echo "SUBTARGET=$SUBTARGET" >> $GITHUB_ENV
          echo "PROFILE=$PROFILE" >> $GITHUB_ENV

      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Build
        run: |
          chmod +x ./scripts/build.sh
          ./scripts/build.sh

      - name: Upload Firmware Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.PLATFORM }}-${{ env.TARGET }}-${{ env.SUBTARGET }}-${{ env.VERSION }}
          path: bin/

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.PLATFORM }}-${{ env.TARGET }}-${{ env.SUBTARGET }}-${{ env.VERSION }}
          body_path: bin/info.md
          files: bin/*
